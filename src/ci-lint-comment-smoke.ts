// Temporary smoke file for CI lint-fix reminder workflow.
// Keep this formatting intentionally "ugly" to trigger ESLint --fix changes in CI.
export const ciLintCommentSmoke = {
  enabled: true,
  note: 'remove after CI reminder test',
};
